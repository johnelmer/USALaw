<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>
  <!-- This page lists down all users -->
  <!-- Only accessible from the admin or head user -->
  <body>
    <h1>Listing articles</h1>
    <table>
      <tr>
        <th>Title</th>
        <th>Text</th>
        <th></th>
      </tr>

      <% @users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.password %></td>
          <td><%= link_to 'Show', user_path(user) %></td>
        </tr>
      <% end %>
    </table>
    <%= link_to 'New User', new_user_path %>
  </body>
</html>
